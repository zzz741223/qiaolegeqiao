# 隐私说明

敲了个敲的设计原则是“统计动作，不读取内容”。项目不包含云端服务、广告 SDK 或用户行为分析 SDK。

## 全局键盘统计

桌面版通过 `uiohook-napi` 接收系统按键按下和松开事件。键码仅在内存中用于：

- 排除 Ctrl、Alt、Shift、Meta、锁定键和截图键等修饰或系统按键
- 防止按住一个键时由系统自动重复计数

应用不会把键码写入磁盘，也不会把键码传给渲染页面或局域网节点。统计文件只包含聚合后的次数和时间桶。

## 本机数据

开发模式数据位于项目目录的 `.dev-user-data/`。Windows 安装版数据位于 `%APPDATA%\敲了个敲\keyboard-stats.json`；macOS 安装版数据位于 `~/Library/Application Support/敲了个敲/keyboard-stats.json`。

文件包含每日聚合统计、累计次数、用户偏好、随机设备 ID 与用户主动收藏的常用搭子。用户可在数据面板清空统计；仅删除应用本体不一定会同时删除这些本地数据。

macOS 版需要用户在“隐私与安全性”中主动授予辅助功能和输入监控权限，这些权限仅用于接收全局键盘按下/松开事件。应用不会借助辅助功能权限读取输入框、界面文本、窗口内容或控制其他应用。

## 局域网数据

“附近工友”默认关闭。用户开启后，应用会：

- 通过 mDNS 在当前局域网广播随机设备 ID、昵称、角色和房间状态
- 开启一个随机端口的本地 WebSocket 服务
- 每秒向附近的敲了个敲设备同步今日累计键数、实时 KPM 和输入状态
- 在工位房内同步局内累计键数、实时 KPM 和在线状态

应用不会通过该连接发送字符、键名、键码、窗口标题、文件内容、剪贴板内容、每小时统计或往日历史。关闭“附近工友”后，mDNS 广播和 WebSocket 服务会停止，今日累计键数也不再对附近设备可见。

## 信任边界

局域网实时榜和房间没有账号认证或端到端加密，适合可信的家庭或办公专用网络。不要在不可信公共 WiFi 中开启“附近工友”。昵称、今日累计键数、实时速度和局内成绩可能被同一网络中的其他敲了个敲用户看到。

## 开源审计

全局键盘处理位于 `electron/main.cjs`，局域网协议位于 `electron/lan-service.cjs`，所有渲染进程可调用的接口都集中在 `electron/preload.cjs`。欢迎通过 Issue 报告隐私或安全问题。
